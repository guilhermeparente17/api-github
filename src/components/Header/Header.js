import React from 'react'

import {
    HeaderContainer,
    HeaderTitle
} from './Header.Elements';

const Header = () => {
  return (
    <HeaderContainer>
        <HeaderTitle>API-GITHUB</HeaderTitle>
    </HeaderContainer>
  )
}

export default Header